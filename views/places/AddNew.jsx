const React = require('react')
import Default from '../Default'

export default function AddNew(){
    return(
        <Default>
            <main>
                <h1>Add new place</h1>
            </main>
        </Default>
    )
}