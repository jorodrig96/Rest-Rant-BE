const React = require('react')
import Default from '../Default'

export default function Show() {
    return(
        <Default>
            <main>
                <h1>Show Page</h1>
            </main>
        </Default>
    )
}